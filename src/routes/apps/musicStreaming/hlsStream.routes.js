const express = require('express');
const hlsStreamController = require('../../../controllers/apps/musicStreaming/hlsStreamController');

const router = express.Router();

// router.route('/hsl/upload').get(musicController.streamSong);

module.exports = router;
